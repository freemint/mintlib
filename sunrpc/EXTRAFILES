# This file gets included by the Makefile in this directory to determine
# the files that should go only into source distributions.
# The files listed here are sorted in alphanumerical order.  This is
# necessary but if it keeps like this the files are also compiled in
# alphabetical order.

SRCFILES += EXTRAFILES Makefile SRCFILES MISCFILES BINFILES \
netname.c publickey.c \
bootparam_prot.x mount.x rex.x rusers.x yppasswd.x key_prot.x \
bootparam_prot.h mount.h rex.h rusers.h yppasswd.h key_prot.h \
nfs_prot.x rquota.x sm_inter.x klm_prot.x nlm_prot.x rstat.x spray.x \
nfs_prot.h rquota.h sm_inter.h klm_prot.h nlm_prot.h rstat.h spray.h \
rpc_parse.h rpc_scan.h rpc_util.h proto.h \
RPCSVCCFILES RPCGENCFILES RPCINFOCFILES
